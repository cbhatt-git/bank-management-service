server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: ${config.server.git.uri:https://gitlab.com/chiranjitbhatt/bank-management-service-config.git}
          clone-on-start: true
